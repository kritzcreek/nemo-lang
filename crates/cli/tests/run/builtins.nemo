module builtins
exports (main)

use prim

import log_float : fn (F32) -> Unit from log_float
import log_int : fn (I32) -> Unit from log_int

fn main() {
    log_float(prim::f32_neg(1.0));
    let xs : Array[I32] = prim::array_new(20, 10);
    log_int(prim::array_len(xs));
}
