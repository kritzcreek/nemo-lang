global x = 10

fn main() -> i32 {
    let y = 20;
    let f = fn (z : i32) -> i32 {
      x + y + z
    };
    twice#[i32](f)(12);
}

fn twice[a](f : fn(a) -> a) -> fn (a) -> a {
    fn (x : a) -> a {
        f(f(x))
    }
}
